@model Reading_Corner.Entities.Student

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="red-font">Create</h2>

<h4>Student</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="FirstName" class="control-label"></label>
                <input asp-for="FirstName" class="form-control" />
                <span asp-validation-for="FirstName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="LastName" class="control-label"></label>
                <input asp-for="LastName" class="form-control" />
                <span asp-validation-for="LastName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Teacher.LName" class="control-label">Teacher</label>
                @Html.DropDownListFor(model => model.TeacherID, (IEnumerable<SelectListItem>)ViewBag.Teachers, new { @class = "form-control" })


                @*@*<select class="form-control" id="exampleSelect1" asp-for="TeacherID">
                @Html.DropDownList("TeacherID", new SelectList(ViewBag.Teachers, "TeacherID", "TeacherName"))
                <option value="11">Finck</option>
                <option value="14">Dixon</option>
                </select>*@
            </div>
            @*<div class="form-group">
                <label asp-for="CurrentBook" class="control-label"></label>
                <input asp-for="CurrentBook" class="form-control" />
                <span asp-validation-for="CurrentBook" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
